# NeuroLoss Submission Labels

Use these descriptions when creating the corresponding Kaggle submissions.

## N1 Dopamine

Short:

`N1 NeuroLoss Dopamine | minimal causal RPE residual`

Long:

`Exact V32 + minimal counterfactual reward-prediction-error correction; tests whether one consistently positive learned residual improves the champion.`

## N2 Hippocampus

Short:

`N2 NeuroLoss Hippocampus | episodic loss-memory rescue`

Long:

`Exact V32 + episodic hard-regime memory retrieval; applies sparse rescue actions when current state resembles historically difficult losses.`

## N3 LC

Short:

`N3 NeuroLoss LC | risk-gated adaptive plasticity`

Long:

`Exact V32 + learned loss-risk adaptive plasticity; becomes more willing to deviate from baseline only in predicted high-risk regimes.`

## N4 CLS

Short:

`N4 NeuroLoss CLS | episodic/generalized consensus gate`

Long:

`Exact V32 + complementary episodic and generalized learning; overrides baseline only when fast memory and slow regime models agree.`

## N5 NeuroStack

Short:

`N5 NeuroStack | regime + episodic + FarmLedger arbitration`

Long:

`Exact V32 + integrated regime risk, episodic memory, counterfactual residuals, and FarmLedger opponent forecasting with conservative arbitration.`